networks:
  beacon-dev:
    external: true

services:
  ian-admin:
    container_name: ian-admin
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    networks:
      - beacon-dev

volumes:
  vscode_extensions:
    external: true
  vscode_data:
    external: true
